/*import React from 'react'
import {useState} from 'react'

export default function AutoEmail(){
const [email, setemail]=useState()
    function cta(evt){
        evt.preventDefault()
        
    }

    return (
        <div>
            <div id="signup">
            <h2 id ="EmailHeader">Automatic Confirmation Email</h2>
            <form action ='/subscribe' method="POST">
                <input type="email" name="email" id="email" placeholder="your email" required pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$"></input>
            <p id="error" class="error">Please enter a valid email</p>
            <input type="submit" value="Notify Me" class="button" id={cta}></input>
            </form>
            </div>
            <div id="success">
                <h2>Confirm your email</h2>
                <p>The last step is to confirm your email. Give bit a couple minutes!</p>
            </div>
        </div>
    )
}*/